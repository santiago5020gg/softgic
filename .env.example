# Copia este archivo a .env y ajusta los valores para tu entorno local.
# NO se versiona el .env real (está en .gitignore). Requisito §3.3: credenciales por variable/secreto.

# SQL Server — la contraseña SA debe ser "fuerte" (>=8, mayúscula, minúscula, dígito y símbolo).
MSSQL_SA_PASSWORD=Str0ng!Passw0rd2026

# Keycloak — usuario administrador de la consola (solo para gestión local del IdP).
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# (Opcional) Override de puertos de host si 8080/8082 están ocupados en tu máquina.
# SVC_SOLICITUDES_PORT=18080
# SVC_INDICADORES_PORT=18082
